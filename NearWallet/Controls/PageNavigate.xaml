<UserControl x:Class="NearWallet.Controls.PageNavigate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NearWallet.Controls"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Margin="20,20,0,0">
    <Grid>
        <Button x:Name="bt_back"
                    Width="34"
                    Height="34"
                    Margin="4 2 2 2"
                    VerticalAlignment="Top"
                    Style="{DynamicResource MahApps.Styles.Button.Circle}" HorizontalAlignment="Left" Click="Bt_back_Click">
            <mah:FontIcon FlowDirection="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}, 
                    Path=FlowDirection, Mode=OneWay}"
                          FontFamily="{DynamicResource MahApps.Fonts.Family.SymbolTheme}"
                          FontSize="20"
                          Glyph="&#xF0B0;" />
        </Button>
    </Grid>
</UserControl>
